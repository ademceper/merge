namespace Merge.Domain.Enums;

/// <summary>
/// Content status values for Blog, CMS, Knowledge Base entities
/// </summary>
public enum ContentStatus
{
    Draft = 0,
    Published = 1,
    Archived = 2,
    Scheduled = 3
}
